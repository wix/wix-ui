{
  "devDependencies": {
    "lerna": "^2.5.0"
  },
  "name": "wix-ui",
  "version": "0.0.1",
  "description": "wix-ui-monorepo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bootstrap": "lerna bootstrap",
    "build": "npm run --prefix packages/wix-ui-test-utils build && npm run --prefix packages/wix-storybook-utils build && npm run --prefix packages/wix-ui-core build && npm run --prefix packages/wix-ui-theme build && npm run --prefix packages/wix-ui-backoffice build && npm run --prefix packages/wix-ui-santa build && npm run --prefix packages/wix-ui-tpa build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wix/wix-ui.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/wix/wix-ui/issues"
  },
  "homepage": "https://github.com/wix/wix-ui#readme"
}
