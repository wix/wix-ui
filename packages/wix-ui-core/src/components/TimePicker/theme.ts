// import {palette} from '../../palette';

export type TimePickerTheme = Partial<{
  rootWidth: string;
  rootHeight: string;
}>;

export const core: TimePickerTheme = {
  rootWidth: 'initial',
  rootHeight: 'initial',
};
