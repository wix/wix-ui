import { StorySettings, Category } from '../../../../stories/utils';

export const storySettings: StorySettings = {
  category: Category.COMPONENTS,
  storyName: 'PopoverNext',
  dataHook: 'story-popover-next',
};
