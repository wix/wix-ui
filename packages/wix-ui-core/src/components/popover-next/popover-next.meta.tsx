import PopoverNext from './popover-next';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(PopoverNext);

metadata.exportedFrom({
  path: 'src/popover-next/popover-next.js',
});
