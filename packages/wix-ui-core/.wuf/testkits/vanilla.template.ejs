module.exports = {
<%
  components
  .filter(component => !component.noVanillaDriver)
  .map(component => {
    const driverPath = component.vanillaDriverPath ||
      [
        './../dist/src/components/',
        ...(component.deprecated ? ['deprecated/'] : []),
        utils.toKebab(component.name),
        '/',
        component.pascalCase ? utils.toPascal(component.name) : component.name,
        '.driver'
      ].join('');
%>  ...require("<%= driverPath %>"),
<% }) %>
};
