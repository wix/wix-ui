sudo: required
dist: trusty
language: node_js
node_js:
  - 8.9.1

before_install:
 - export CHROME_BIN=/usr/bin/google-chrome
 - export DISPLAY=:99.0
 - sh -e /etc/init.d/xvfb start
 - sudo apt-get update
 - sudo apt-get install -y libappindicator1 fonts-liberation
 - wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
 - sudo dpkg -i google-chrome*.deb
 
script:
 - npm run bootstrap && npm run test
 - echo $TRAVIS_BRANCH
 - echo $TRAVIS_PULL_REQUEST
 - echo $TRAVIS_PULL_REQUEST_BRANCH
 - echo $TRAVIS_PULL_REQUEST_SLUG
#  - 'if [ "$TRAVIS_PULL_REQUEST" != "false" ]; then bash ./scripts/travis-on-pull-request.sh; fi'
 - npm install surge
 - 'if [ "$TRAVIS_PULL_REQUEST" != "false" ]; then bash ./scripts/surge-deploy.sh; fi'

# deploy:
#   provider: s3
#   access_key_id: $AWS_ACCESS_KEY_ID
#   secret_access_key: $AWS_SECRET_ACCESS_KEY
#   bucket: $S3_BUCKET
#   skip_cleanup: true
#   upload-dir: '/home/travis/build/wix/wix-ui/packages/wix-ui-core/storybook-static'
#   on:
#     all_branches: true
#     condition: $TRAVIS_PULL_REQUEST" != "false"

notifications:
  email:
    recipients:
      - fed-infra@wix.com
